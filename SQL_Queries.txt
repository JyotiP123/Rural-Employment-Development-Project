mysql> use masaiProject;
Database changed

-- bdo table creation

CREATE TABLE BDO ( 
	ID INT(10)NOT NULL, 
	USERNAME VARCHAR(30), 
	PASSWORD VARCHAR(20));
    
-- gram panchayat table

 CREATE TABLE GPM(
     GID INT(10) PRIMARY KEY,
     GNAME VARCHAR(20) NOT NULL,
     GEMAIL VARCHAR(30) NOT NULL,
     GPASSWORD VARCHAR(20) NOT NULL,
     GMOBILE VARCHAR(12) NOT NULL,
     GADDRESS VARCHAR(30) NOT NULL,
     BDOID INT(10) NOT NULL);


-- employee data table

CREATE TABLE EMP(
     EID INT(10) PRIMARY KEY,
     ENAME VARCHAR(20) NOT NULL,
     EMOBILE VARCHAR(20) NOT NULL,
     EADDRESS VARCHAR(30) NOT NULL,
     ETOTALDAYWORK INT(10) NOT NULL,
     EWAGES VARCHAR(10) NOT NULL);

    
-- project data table

 CREATE TABLE PROJECT(
    PID INT(10) NOT NULL,
    PCOST VARCHAR(20) NOT NULL,
    PNAME VARCHAR(20),
    GPMID INT(10));

-- employee-project join table

 CREATE TABLE EMPDTO(
     ENAME VARCHAR(20) NOT NULL,
     EMOBILE VARCHAR(20) NOT NULL,
     EADDRESS VARCHAR(30) NOT NULL,
     ETOTALDAYWORK INT(10) NOT NULL,
     PNAME VARCHAR(20) PRIMARY KEY);
     

-- applying foreign keys 

ALTER TABLE PROJECT
ADD FOREIGN KEY (PNAME) REFERENCES EMPDTO(PNAME);

ALTER TABLE PROJECT
ADD FOREIGN KEY (GPMID) REFERENCES GPM(GID);
